import React from "react";
import { Container, Box, Button } from "@mui/material";
import TuitionDetailsHeader from "../StudentPannel/TuitionDetailsHeader";
import TuitionMetaInfo from "../StudentPannel/TuitionMetaInfo";
import TuitionCompletionStats from "../StudentPannel/TuitionCompletionStats";
import MenuBookIcon from '@mui/icons-material/MenuBook';         // Course Icon
import PictureAsPdfIcon from '@mui/icons-material/PictureAsPdf'; // PDF Icon
import AssignmentTurnedInIcon from '@mui/icons-material/AssignmentTurnedIn'; // Test Icon


const TuitionDetailsPage = () => {
  return (
    <Box sx={{ py: 4 }}>
      <TuitionDetailsHeader title="Year 3 11+ Tuition" />
      <TuitionMetaInfo
        image="/assets/images/studend-img.png"
        description="Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry's standard dummy text..."
        duration="12 hours"
        timeTaken="5 hours"
        progress={80}
      />
     <Box mt={3}>
  <Button
    variant="contained"
    startIcon={<MenuBookIcon />}
    sx={{
      mr: 2,
      borderRadius: 2,
      textTransform: "none",
      fontWeight: 600,
      bgcolor: "#26177C",
      "&:hover": { bgcolor: "#1b1162" }
    }}
  >
    Course
  </Button>

  <Button
    variant="outlined"
    startIcon={<PictureAsPdfIcon />}
    sx={{
      mr: 2,
      borderRadius: 2,
      textTransform: "none",
      fontWeight: 600,
      borderColor: "#ccc",
      color: "#000",
      "&:hover": { bgcolor: "#f0f0f0", borderColor: "#aaa" }
    }}
  >
    Pdf
  </Button>

  <Button
    variant="outlined"
    startIcon={<AssignmentTurnedInIcon />}
    sx={{
      borderRadius: 2,
      textTransform: "none",
      fontWeight: 600,
      borderColor: "#ccc",
      color: "#000",
      "&:hover": { bgcolor: "#f0f0f0", borderColor: "#aaa" }
    }}
  >
    Test
  </Button>
</Box>

      <TuitionCompletionStats />
    </Box>
  );
};

export default TuitionDetailsPage;
